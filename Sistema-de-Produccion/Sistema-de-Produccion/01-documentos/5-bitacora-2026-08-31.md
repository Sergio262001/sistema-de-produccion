# Bitácora · sesión del 31 ago – 1 sep 2026

Qué se construyó, qué se rompió, qué se aprendió y qué sigue.
Escrito para que **la siguiente sesión no tenga que redescubrir nada.**

> Si eres el asistente de la próxima sesión: lee este archivo completo antes
> de tocar código. La sección **"Cómo trabajar en este proyecto"** al final
> tiene las reglas que salieron de equivocarme, no de la teoría.

---

## Estado al cerrar

| | |
|---|---|
| Repositorio | `github.com/Sergio262001/sistema-de-produccion` · **privado** |
| Commits | 20, todos subidos |
| Pruebas | **174**, todas pasando (`cd herramientas && npm test`) |
| Validador sobre la fábrica | 0 errores |
| Panel | `cd herramientas && node panel.js` → http://localhost:4321 |
| Formulario del cliente | `herramientas/brief.html` (doble clic, o `/brief.html`) |
| Agente de Claude | **funcionando**, probado con API real (~$0.03/sesión con Haiku) |

Verificado clonando el remoto en limpio: `npm install` + `npm test` + generar
un proyecto + `npm run dev` funcionan sin nada del entorno local.

---

## Lo que había antes de esta sesión

El sistema de producción existía: 10 bases técnicas, prompts maestros,
plantillas de negocio. **Pero no estaba en git** (solo OneDrive), no tenía
pruebas, y el `CLAUDE.md` bueno estaba enterrado dos niveles abajo, así que
no se cargaba nunca al abrir Claude Code desde el escritorio.

---

## Lo que se construyó

### `herramientas/` — la fábrica operable (nueva)

Cero dependencias en todo el modo gratis. Node y nada más.

| Archivo | Qué hace |
|---|---|
| `panel.js` + `panel.html` | Servidor local. Acceso, asistente, proyectos, prompts, calidad, agente |
| `brief.html` | Formulario que le mandas al cliente. Autocontenido |
| `validar.js` | Control de calidad determinista, $0 |
| `crear-proyecto.js` | Genera el proyecto del cliente, ejecutable |
| `auditor-ia.js` | Auditoría de criterio, con costo estimado antes |
| `lib/reglas.js` | Las 9 reglas del validador |
| `lib/brief.js` | El cuestionario y la compuerta de ficha |
| `lib/contenido.js` | Catálogo del cliente → semilla del entregable |
| `lib/extraer.js` | Documento → respuestas (patrones o IA) |
| `lib/agente.js` | Claude Agent SDK con guardrails |
| `lib/historial.js` | `node:sqlite`, historial y gasto |
| `lib/acceso.js` · `lib/google.js` | Frase con scrypt · OAuth de Google |
| `lib/colores.js` · `lib/yaml.js` · `lib/prompts.js` | Contraste WCAG · fichas · prompts maestros |

### Cambios en la fábrica (`02-bases/`)

- **Fotos de producto** en `ecommerce-completo` y `menu-con-panel-admin`:
  columna `imagen`, `<img>` con `alt` real y carga diferida, emoji como
  respaldo, `urlSegura()` filtrando el `src`.
- **Direcciones de arte** en `ecommerce-completo`: `mercado`, `boutique`,
  `taller`. Cambian tipografía, escala, forma y ritmo — no solo color.
  La demo trae un conmutador para verlas en vivo con el cliente.
- **Los 5 bloques que faltaban** en `prompt-por-base.md` (ahora 10 de 10).
  **Son borradores míos: hay que revisarlos**, codifican criterio del dueño.

---

## Los bugs. Esta es la parte que importa

**Los cinco bugs que de verdad importaron los encontró el dueño abriendo la
página. Ninguno lo encontré yo leyendo código.** El validador daba 0 errores
en todos los casos.

| # | Bug | Qué lo delataba |
|---|---|---|
| 1 | Página en blanco: se borró la sysbar y con ella unos `id` que el script pedía. `getElementById` devuelve `null`, la línea lanza, muere todo el JS | **Nada.** Tamaños normales, 0 hallazgos, la página cargaba |
| 2 | Página en blanco: al inyectar código con `bash node -e`, la regex de URL perdió sus barras invertidas y el `<script>` dejó de compilar | Nada |
| 3 | El entregable decía "Café Raíz" siendo de "tacos mauricio": `applyTheme()` lee el `CONTEXT` del script y lo sobreescribe todo al arrancar | Nada |
| 4 | De 19 respuestas del brief sobrevivían 5: `crearProyecto` solo recibía 4 campos y el resto se marcaba `POR DEFINIR` | Nada |
| 5 | El formulario saltaba del paso 2 al último: se usó `NO_APLICA` sin declararla nunca | Nada. **El archivo compila** — reventaba al ejecutar |

**El patrón:** cuatro de los cinco los introduje yo, en el mismo commit en que
decía "verificado". Verificaba estructura, no funcionamiento.

### Las reglas que salieron de ahí

Cada bug dejó una regla en `lib/reglas.js`:

- **`id-fantasma`** — `getElementById` de un id que no existe. Si se encadena
  (`.textContent`), es **error**, porque tumba el script entero.
- **`sintaxis`** — cada `<script>` tiene que compilar (`new Function`).
- **`pruebas/formulario.test.js`** — monta un DOM y **ejecuta** el formulario,
  recorriendo los 6 pasos. Nació del bug 5, que compilaba perfectamente.

Y tres veces me atrapó mi propio validador antes de commitear: dos tokens
CSS muertos (`--hueco`, `--sup3`) y la tercera barra invertida rota.

---

## Lo que sigue sin estar hecho

### Sin pruebas — la brecha más incómoda

```
panel.js        las ~20 rutas del servidor
lib/acceso.js   ES EL LOGIN. Código de seguridad, cero pruebas
lib/google.js   probado solo con respuestas simuladas
lib/agente.js   puede escribir archivos
lib/prompts.js  auditor-ia.js
```

142 pruebas y **ninguna toca el código de seguridad**. Empezar por `acceso.js`:
es media hora y quita el peor hueco.

### Deuda conocida

- **El formulario del cliente NO pide el catálogo.** Descubierto el
  2026-09-01. La pregunta `catalogo` existe en `lib/brief.js` (y por eso
  funciona el asistente del panel), pero `brief.html` nunca la dibuja: no
  está en `P` ni en los campos del paso de contenido. Consecuencia: **todo
  brief que llene un cliente produce un entregable con el catálogo de
  ejemplo.** Es el mismo fallo que se arregló para el asistente, sin
  arreglar en el otro camino. Debería ser lo próximo — es una hora y es la
  razón principal de que las entregas se vean "de plantilla".

- **`prueba-ecommerce` sigue con `localAuth`.** Era el único pendiente técnico
  cuando empezó todo esto, en el `CLAUDE.md` original. Sigue igual: el login es
  local y las políticas RLS exigen sesión real de Supabase Auth, así que
  guardar desde el panel falla.
- **Contraseña de demo en el código.** El entregable trae
  `admin@<slug>.local / admin123` impreso en la pantalla de login. Se cambió el
  dominio para no mostrar el negocio de otro, pero **el fondo no está resuelto**.
- **`panel.html` = 1.144 líneas** en un archivo.
- **`backend-pro` nunca se probó contra Wompi real.**
- **Doble carpeta** `Sistema-de-Produccion/Sistema-de-Produccion/`.
- **`08-pagina-del-estudio`** tiene 4 copias de 2 archivos.
- **Fotos**: 2 de 9 bases. **Direcciones de arte**: 1 de 9.
- **Los proyectos viejos de `Proyectos-Clientes/` están rotos** (generados antes
  de los arreglos). Regenerarlos o borrarlos.
- **La API key se pegó en el chat.** El dueño decidió no rotarla. Es su
  llamada; queda anotado.

---

## Plan para la siguiente sesión

El plan completo está en [`4-plan-de-mejoras.md`](./4-plan-de-mejoras.md).
Esto es el orden concreto de lo que queda:

### ~~1. Logo y banner~~ — HECHO el 2026-09-01

`marca.logo` y `marca.banner` recorren el sistema entero: pregunta en el
formulario del cliente (con vista previa), pregunta en el asistente del panel,
normalización y avisos en `lib/brief.js`, `--logo`/`--banner` en el CLI,
inyección en el `CONTEXT` del entregable, y `pintarLogo()`/`pintarBanner()` en
`ecommerce-completo` y `menu-con-panel-admin`. Sin logo se pinta la inicial;
sin banner no se muestra nada. Todo pasa por `urlSegura()`.

Tres cosas que dejó de propina:

- **`pruebas/marca.test.js`** ejecuta las dos funciones tal como viajan en el
  `demo.html` (las extrae del archivo, no una copia).
- **Conmutador `Inicial ↔ Logo y banner`** en la sysbar de las dos demos, con
  SVG locales: se ve la diferencia con doble clic y sin internet.
- **Marcadores `@demo-only` / `@fin-demo`**: `crear-proyecto.js` borra lo que
  quede entre ellos. Borrar la sysbar quitaba los botones pero dejaba vivas
  las funciones que llamaban — código muerto apuntando a archivos de ejemplo.

Falta llevarlo a las otras 7 bases (va con el punto 5).

### 2. Pruebas de `lib/acceso.js`

Media hora. Es el login y no tiene ninguna.

### 3. Preguntas de contenido para las bases que faltan

Ya funciona para menú y tienda (`lib/contenido.js`). Falta:

- **Landing**: qué secciones y el texto del hero.
- **CRM**: qué estados usa el negocio si no le sirven los tres por defecto.

### 4. Editor de contenido en el panel del entregable

Añadir, renombrar y reordenar categorías; editar los textos de la página;
subir imágenes en vez de pegar una URL.

**Alcance honesto: es un editor de campos, no WordPress.** Sin plantillas, sin
plugins, sin revisiones, sin multiusuario.

### 5. Llevar fotos y direcciones de arte a las bases que faltan

Prioridad `carrito-reutilizable` y `marketplace`, por ser de venta.

### 6. Cerrar `prueba-ecommerce`

Cambiar `src/main.js` para que el login use `supabase.auth.signInWithPassword`
y crear el usuario en Authentication → Users.

### Lo que NO hay que construir

Un WordPress de verdad, editor de arrastrar y soltar, más direcciones de arte,
multiusuario, servidor de base de datos, contenedores. El estudio es una
persona: eso multiplicaría el trabajo sin devolver nada.

---

## Cómo trabajar en este proyecto

Reglas que salieron de equivocarme en esta sesión. No son teoría.

1. **Si tocas un HTML, ábrelo.** Que compile no es que funcione. Cuatro de los
   cinco bugs se entregaron con el validador en verde.

2. **No inyectes código con `bash node -e` ni con heredocs.** Las barras
   invertidas de las expresiones regulares se pierden por el camino. Pasó
   **tres veces** en una sola sesión. Usa las herramientas de edición de
   archivos.

3. **Corre `npm test` antes de cada commit.** Y `node validar.js ..` también.

4. **Cuando arregles un bug, escribe la regla que lo habría cazado.** Así
   nacieron `id-fantasma`, `sintaxis` y las pruebas del formulario.

5. **Nunca inventes datos del cliente.** Es la regla del sistema entero. Si
   falta algo, se marca `POR DEFINIR` o se omite — nunca se rellena con el
   ejemplo. Un dato inventado se nota en la entrega.

6. **Prueba las dos caras de cada regla.** Un validador con falsos positivos
   se deja de mirar, y entonces no sirve de nada.

7. **El dueño encuentra los bugs de verdad.** Cuando diga que algo se ve mal,
   **empieza por creerle** y reproducirlo, no por explicar por qué debería
   funcionar.
